<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esame.suerzgabriele">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".Data.ReservationContentProvider"
            android:authorities="com.esame.suerzgabriele" />

        <activity android:name=".Activity.Activity2" />
        <activity android:name=".Activity.Activity3" />
        <activity android:name=".Activity.Activity4"></activity>
    </application>

</manifest>