<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gabrysuerz.esame_2015">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.HOME01">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.NEW01" />
        <activity android:name=".Activity.NEW02" />
        <activity android:name=".Activity.LIST01" />
        <activity android:name=".Activity.LIST02" />

        <provider
            android:name=".Data.TrainingContentProvider"
            android:authorities="com.gabrysuerz.esame_2015"
            android:exported="false" />

        <service
            android:name=".TimerService"></service>
    </application>

</manifest>